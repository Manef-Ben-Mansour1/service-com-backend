import { ExcludeTimestampInterceptorInterceptor } from './exclude-timestamp-interceptor.interceptor';

describe('ExcludeTimestampInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new ExcludeTimestampInterceptorInterceptor()).toBeDefined();
  });
});
